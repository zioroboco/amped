import * as React from "react"
import { hot } from "react-hot-loader"

const App = (): JSX.Element => (
  <div style={{ textAlign: "center", margin: "6rem", fontSize: "12rem" }}>
    ⚡️
  </div>
)

export { App }
